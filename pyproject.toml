[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tallyho"
version = "0.1.0"
description = "..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "topytxt",
]